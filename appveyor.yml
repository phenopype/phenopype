image:
- Ubuntu1804
- Visual Studio 2019
- macOS

branches:
  only:
    - development

environment:
  matrix:
    - PYTHON: "C:\\Python33"
      PYTHON_VERSION: "3.7"

build:
  off

install:
- pip install pytest
- pip install pytest-cov
- pip install coveralls
- pip install .
  
test_script:
- pytest --cov

after_test:
- coveralls
