- preprocessing:
  - create_mask:
      tool: polygon
      label: mask1
  - blur:
      kernel_size: 9
- segmentation:
  - threshold:
      method: adaptive
      blocksize: 99
      constant: 5
      channel: red
      df_masks: mask1
  - detect_contours:
      retrieval: ext
      min_area: 150
- measurement:
  - shape_features:
      return_moments: true
- visualization:
  - select_canvas:
      canvas: raw
  - draw_contours
  - draw_masks
- export:
  - save_canvas:
      overwrite: true
  - save_contours
  - save_masks
  - save_shapes