os:
  - linux
#  - osx       ## no travis support
#  - windows   ## no travis support 
language: python
python:
  - "3.7"
install:
  - pip install pytest
  - pip install pytest-cov
  - pip install coveralls
  - pip install .
script:
  - pytest
after_success:
  - coverage run
  - coveralls
