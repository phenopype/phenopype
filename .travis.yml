os:
- linux
dist: xenial
services:
  - xvfb
  
language: python
python:
- "3.7"
install:
- python -m pip install -U pip
- pip install tox
script:
- sudo rm /tmp/.tX99-lock
- xvfb-run -e /dev/stdout tox
